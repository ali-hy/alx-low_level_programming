#include <stdio.h>

/**
 * print_alphabet - print the alphabet in lower case letters
 *
 * Return: Always 0 (Success)
 */
void print_alphabet()
{
	char c;
	
	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar('a');
	}
	_putchar('\n');
}
