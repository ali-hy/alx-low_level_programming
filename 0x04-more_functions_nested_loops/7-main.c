#include <stdio.h>
#include "main.h"

/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_diagonal(1);
	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	return (0);
}
