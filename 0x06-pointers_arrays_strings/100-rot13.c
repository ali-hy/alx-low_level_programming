#include <stdio.h>
#include "main.h"

/**
 * rot13 - encode string using rot13
 * @s: string
 * Return: resultant string
 */
char *rot13(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		if ((s[i] >= 'a' && s[i] <= 'z') || (s[i] >= 'A' && s[i] <= 'Z'))
		{
			
		}
	}
}
